name: Run Tests and Deploy Bot

on:
  push:
    branches: [master] # или main, если у тебя другая ветка

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Клонируем репозиторий
        uses: actions/checkout@v3

      - name: 🐍 Устанавливаем Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: 📦 Устанавливаем зависимости
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install black  # добавим black на всякий случай

      - name: 🔍 Проверка форматирования Black
        run: |
          echo "Проверка форматирования кода..."
          black --check .

      - name: ✅ Запускаем тесты (unittest)
        run: |
          echo "Запуск тестов..."
          python -m unittest discover -s tests

      - name: 🚀 Деплой (эмуляция запуска бота)
        if: success()
        run: |
          echo "Тесты и форматирование пройдены. Запускаем бота..."
          timeout 10s python app/main.py || echo "Бот завершён"
